const std = @import("std")

const greeting: string = "Hello, {}! From {}"
let name = "Foo"
let year = 2023

$ fn (string, string, isize) -> string
const greet = (greeting, name, year) => {
  return std/fmt.string(greeting, name, year)
}
